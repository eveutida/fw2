import { Container } from "react-bootstrap";
import styles from "@/styles/Footer.module.css"; // Verifique se o caminho está correto

export default function Footer() {
    return (
        <Container className="position-button bottom-0 start-50">
            {/* Aqui você pode adicionar seu conteúdo */}
        </Container>
    );
}
